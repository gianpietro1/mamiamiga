= form_for @contact_form do |f|  
  .contact_form
    .row
      .col-md-4
        .form_group
          = f.label 'Nombre'
          %span.red
            *
          = f.text_field :contact_name, class: 'form-control', :required => true, :title => 'Por favor llena este campo.'
        .form_group
          = f.label 'Correo Electrónico'
          %span.red
            *
          = f.text_field :contact_email, class: 'form-control', :required => true, :title => 'Por favor llena este campo.'
        .form_group
          = f.label 'Comentarios'
          = f.text_area :contact_comments, class: 'form-control', :required => true, :title => 'Por favor llena este campo.'
        %p
        .form_group.pull-left
          = link_to 'Cancelar', root_path, class: "btn btn-danger"
          = f.submit 'Enviar', class: 'btn btn-success'

